# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง (อย่า commit .env)
# สร้าง JWT_SECRET แบบสุ่ม เช่น: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=เปลี่ยนเป็นค่าสุ่มยาวๆ
PORT=3000
